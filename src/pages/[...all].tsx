import React from 'react';
import type { FC } from 'react';

const all: FC = () => {
    return (
        <h2
            style={{
                height: 'calc(100vh - 536px)',
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center'
            }}
        >
            ...all route: 404
        </h2>
    );
};

export default all;
